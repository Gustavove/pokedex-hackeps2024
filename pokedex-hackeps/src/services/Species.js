

// src/services/Species.js

class Species {
    constructor(name, url) {
        this.name = name; // Nombre de la especie (como 'Pikachu', 'Charmander', etc.)
        this.url = url; // URL relacionada a la especie
    }
}

export default Species;